// experimenter_overview/config.js - Configuration for the Experimenter Overview

const OVERVIEW_CONFIG = {
    // Add participant IDs to this set to exclude them from the overview
    EXCLUDED_PPTS: new Set([''])
};